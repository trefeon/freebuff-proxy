[
  {
    "key": "ACTING_USER_ID",
    "group": "general",
    "kind": "text",
    "default": "",
    "restart_only": true,
    "secret": false,
    "description": "Optional FreeBuff account id sent as x-freebuff-acting-user-id. BAN RISK: only the token's own account id is safe (any other value impersonates another user). Pre-rename name USER_ID still works. Empty = header omitted.",
    "essential": false
  },
  {
    "key": "AUTO_DISCOVER_TOKEN",
    "group": "general",
    "kind": "bool",
    "default": "true",
    "restart_only": false,
    "secret": false,
    "description": "When AUTH_TOKENS is empty, read credentials from the official CLI login files (false disables). Environment-only: it controls whether .env is read at all, so a .env line is inert outside Docker.",
    "essential": true
  },
  {
    "key": "CLI_VERSION",
    "group": "general",
    "kind": "text",
    "default": "0.10.7",
    "restart_only": false,
    "secret": false,
    "description": "Upstream CLI version string; informational only — parsed and shown on the dashboard, no wire impact.",
    "essential": false
  },
  {
    "key": "DASHBOARD_ENABLED",
    "group": "general",
    "kind": "bool",
    "default": "true",
    "restart_only": false,
    "secret": false,
    "description": "Serve the embedded admin dashboard at /admin (false disables all /admin routes with 404).",
    "essential": true
  },
  {
    "key": "DEBUG_DUMP",
    "group": "general",
    "kind": "bool",
    "default": "false",
    "restart_only": true,
    "secret": false,
    "description": "Persist redacted traffic dumps to ./dump/ (mode 0600, debugging only).",
    "essential": false
  },
  {
    "key": "DEVTOOLS_ENABLED",
    "group": "general",
    "kind": "bool",
    "default": "false",
    "restart_only": false,
    "secret": false,
    "description": "Show the Dev Tools page (batch chat, session spawner) in the admin dashboard. Default off: it is a manual testing surface that hammers /v1/*.",
    "essential": false
  },
  {
    "key": "LISTEN_ADDR",
    "group": "general",
    "kind": "text",
    "default": "127.0.0.1:3457",
    "restart_only": false,
    "secret": false,
    "description": "Host and port to bind (loopback by default; containers set :3457 for all interfaces).",
    "essential": true
  },
  {
    "key": "LOG_ACCESS",
    "group": "general",
    "kind": "bool",
    "default": "true",
    "restart_only": false,
    "secret": false,
    "description": "Log one access line per HTTP request (healthz/metrics/OPTIONS are rate-limited to 1/min regardless).",
    "essential": false
  },
  {
    "key": "LOG_FILE",
    "group": "general",
    "kind": "text",
    "default": "",
    "restart_only": false,
    "secret": false,
    "description": "Append log lines to a file (e.g. ./logs/proxy.log); empty = stderr.",
    "essential": true
  },
  {
    "key": "LOG_FORMAT",
    "group": "general",
    "kind": "select",
    "default": "",
    "restart_only": false,
    "secret": false,
    "description": "",
    "enum": [
      "text",
      "json"
    ],
    "essential": false
  },
  {
    "key": "LOG_LEVEL",
    "group": "general",
    "kind": "select",
    "default": "",
    "restart_only": false,
    "secret": false,
    "description": "",
    "enum": [
      "debug",
      "info",
      "warn",
      "error",
      "trace"
    ],
    "essential": true
  },
  {
    "key": "LOG_RING_SIZE",
    "group": "general",
    "kind": "int",
    "default": "500",
    "restart_only": false,
    "secret": false,
    "description": "In-memory log ring capacity behind the dashboard log viewer (50-5000).",
    "essential": false
  },
  {
    "key": "SAFE_MODE",
    "group": "general",
    "kind": "bool",
    "default": "true",
    "restart_only": false,
    "secret": false,
    "description": "Apply the anti-ban preset when a knob is left unset: idle-run rotation 30m, request jitter 2s, browser TLS fingerprint auto. Keep on.",
    "essential": true
  },
  {
    "key": "ADOPT_CLI_SESSION",
    "group": "pool",
    "kind": "bool",
    "default": "false",
    "restart_only": true,
    "secret": false,
    "description": "Adopt the upstream CLI's active session (from freebuff-instance-owner.json) instead of creating a new one; with empty AUTH_TOKENS the token is sourced from the CLI credentials file.",
    "essential": false
  },
  {
    "key": "API_KEYS",
    "group": "pool",
    "kind": "list",
    "default": "",
    "restart_only": false,
    "secret": true,
    "description": "Comma-separated client keys required for /v1/* (empty = open; ignored in bridge mode). In hybrid mode API_KEYS is the discriminator: a matching credential uses the pool, any other is relayed upstream as a bridge token.",
    "essential": false
  },
  {
    "key": "AUTH_TOKENS",
    "group": "pool",
    "kind": "list",
    "default": "",
    "restart_only": false,
    "secret": true,
    "description": "Comma-separated upstream FreeBuff tokens. EMPTY = bridge mode: the proxy stores no tokens and each client sends its own; set = pooled (or hybrid with BRIDGE_ENABLED).",
    "essential": false
  },
  {
    "key": "BRIDGE_ENABLED",
    "group": "pool",
    "kind": "bool",
    "default": "true",
    "restart_only": false,
    "secret": false,
    "description": "With AUTH_TOKENS set, accept bridge-mode clients (their own token relayed) alongside the pool — hybrid mode. 0 = locked-down pooled-only instance.",
    "essential": true
  },
  {
    "key": "BRIDGE_IDLE_EVICT",
    "group": "pool",
    "kind": "text",
    "default": "72h",
    "restart_only": false,
    "secret": false,
    "description": "How long a bridge entry may sit unused before its runs are FINISHed and it is evicted from the cache (sliding TTL; zero or invalid → 72h).",
    "essential": false
  },
  {
    "key": "IDLE_ROTATION_TIMEOUT",
    "group": "pool",
    "kind": "text",
    "default": "0",
    "restart_only": false,
    "secret": false,
    "description": "Finish runs after this idle period (0 = disabled; SAFE_MODE sets 30m when unset).",
    "essential": false
  },
  {
    "key": "MODEL_UNAVAILABLE_CACHE_TTL",
    "group": "pool",
    "kind": "text",
    "default": "1h",
    "restart_only": false,
    "secret": false,
    "description": "How long a model_unavailable admission refusal is remembered per model (off-window models short-circuit to the fallback within the TTL).",
    "essential": false
  },
  {
    "key": "RATE_LIMIT_BURST",
    "group": "pool",
    "kind": "int",
    "default": "0",
    "restart_only": false,
    "secret": false,
    "description": "Burst request capacity per client IP (0 = defaults to 2 × RATE_LIMIT_PER_IP).",
    "essential": false
  },
  {
    "key": "RATE_LIMIT_PER_IP",
    "group": "pool",
    "kind": "text",
    "default": "0",
    "restart_only": false,
    "secret": false,
    "description": "Requests/second allowed per client IP (0 = disabled; e.g. 20).",
    "essential": true
  },
  {
    "key": "ROTATION_INTERVAL",
    "group": "pool",
    "kind": "text",
    "default": "6h",
    "restart_only": false,
    "secret": false,
    "description": "Agent-run rotation interval.",
    "essential": false
  },
  {
    "key": "RUNS_DRAIN_QUEUE_CAP",
    "group": "pool",
    "kind": "int",
    "default": "64",
    "restart_only": false,
    "secret": false,
    "description": "Draining-runs list cap; entries beyond it are force-dropped (FINISH is best-effort).",
    "essential": false
  },
  {
    "key": "RUNS_DRAIN_TTL",
    "group": "pool",
    "kind": "text",
    "default": "10m",
    "restart_only": false,
    "secret": false,
    "description": "Draining-runs TTL eviction window.",
    "essential": false
  },
  {
    "key": "RUN_FINISH_INLINE_TIMEOUT",
    "group": "pool",
    "kind": "text",
    "default": "250ms",
    "restart_only": false,
    "secret": false,
    "description": "Synchronous inline FINISH fallback bound when the deferred-FINISH queue is full.",
    "essential": false
  },
  {
    "key": "RUN_FINISH_QUEUE_SIZE",
    "group": "pool",
    "kind": "int",
    "default": "64",
    "restart_only": false,
    "secret": false,
    "description": "Bounded deferred-FINISH worker queue for rotated/drained runs (full queue falls back to a synchronous FINISH).",
    "essential": false
  },
  {
    "key": "SESSION_CREATE_MAX_PARALLEL_GLOBAL",
    "group": "pool",
    "kind": "int",
    "default": "128",
    "restart_only": false,
    "secret": false,
    "description": "Cap on concurrent in-flight session admissions (wait-or-503) across all models.",
    "essential": false
  },
  {
    "key": "SESSION_CREATE_MAX_PARALLEL_PER_MODEL",
    "group": "pool",
    "kind": "int",
    "default": "32",
    "restart_only": false,
    "secret": false,
    "description": "Per-model cap on concurrent in-flight session admissions.",
    "essential": false
  },
  {
    "key": "SESSION_IDLE_END",
    "group": "pool",
    "kind": "text",
    "default": "0",
    "restart_only": false,
    "secret": false,
    "description": "End upstream sessions after this idle period, releasing the token's daily admission slot while the proxy sits unused (0 = disabled, opt-in).",
    "essential": false
  },
  {
    "key": "SESSION_PERSIST",
    "group": "pool",
    "kind": "bool",
    "default": "true",
    "restart_only": true,
    "secret": false,
    "description": "Persist session state AND active agent runs to disk so a restart resumes them instead of re-creating (default true). Set to false to disable.",
    "essential": false
  },
  {
    "key": "SESSION_PROBE_CACHE_TTL",
    "group": "pool",
    "kind": "text",
    "default": "15s",
    "restart_only": false,
    "secret": false,
    "description": "Reuse the last successful session state (skip redundant session poll GETs) within this window.",
    "essential": false
  },
  {
    "key": "SESSION_RE_ADMIT_LEAD",
    "group": "pool",
    "kind": "text",
    "default": "60s",
    "restart_only": false,
    "secret": false,
    "description": "Re-admit a session pre-emptively when less than this remains: the request rides the old session while the refresh runs in the background.",
    "essential": false
  },
  {
    "key": "SESSION_STATE_FILE",
    "group": "pool",
    "kind": "text",
    "default": ".freebuff-session-state.json",
    "restart_only": true,
    "secret": false,
    "description": "Path of the session state file (used when SESSION_PERSIST=true; token-keyed, 0600, raw tokens never written).",
    "essential": false
  },
  {
    "key": "TOKEN_ROTATION",
    "group": "pool",
    "kind": "select",
    "default": "",
    "restart_only": false,
    "secret": false,
    "description": "",
    "enum": [
      "drain",
      "round_robin",
      "least_used",
      "random"
    ],
    "essential": true
  },
  {
    "key": "WAITING_ROOM_CHAIN",
    "group": "pool",
    "kind": "bool",
    "default": "false",
    "restart_only": false,
    "secret": false,
    "description": "After an upstream 428 waiting_room_required, fire the reference ad-chain + streak requests before the next session create (best-effort, never blocks).",
    "essential": false
  },
  {
    "key": "BRIDGE_DAILY_LIMIT",
    "group": "quota",
    "kind": "int",
    "default": "0",
    "restart_only": false,
    "secret": false,
    "description": "Global daily chat cap across ALL bridge-mode entries (0 = unlimited).",
    "essential": false
  },
  {
    "key": "FALLBACK_AFTER_MS",
    "group": "quota",
    "kind": "int",
    "default": "10000",
    "restart_only": false,
    "secret": false,
    "description": "Queue-wait threshold (ms) before re-routing to FALLBACK_MODEL; 0 disables fallback. Queue-wait only — never on 429 quota exhaustion.",
    "essential": true
  },
  {
    "key": "FALLBACK_MODEL",
    "group": "quota",
    "kind": "list",
    "default": "",
    "restart_only": false,
    "secret": false,
    "description": "Map model=fallback (comma-separated) to re-route a request when its queue wait passes FALLBACK_AFTER_MS. Unset default: openai/gpt-5.6-luna → deepseek/deepseek-v4-flash.",
    "essential": false
  },
  {
    "key": "MAX_MESSAGES_PER_DAY",
    "group": "quota",
    "kind": "int",
    "default": "0",
    "restart_only": false,
    "secret": false,
    "description": "Per-token daily cap on successful chats (0 = unlimited; the upstream 429 lock is the real enforcement).",
    "essential": true
  },
  {
    "key": "MAX_SPEND_PER_DAY",
    "group": "quota",
    "kind": "int",
    "default": "0",
    "restart_only": false,
    "secret": false,
    "description": "Advisory per-token Pacific-day spend ceiling in ledger units (0 = unlimited). Never enforced — surfacing only, on /healthz.",
    "essential": false
  },
  {
    "key": "QUOTA_FALLBACK_MODELS",
    "group": "quota",
    "kind": "list",
    "default": "deepseek/deepseek-v4-flash=mimo/mimo-v2.5,z-ai/glm-5.2=deepseek/deepseek-v4-flash,openai/gpt-5.6-luna=deepseek/deepseek-v4-flash",
    "restart_only": false,
    "secret": false,
    "description": "Map model → fallback when its session quota is exhausted or unentitled (comma-separated k=v pairs).",
    "essential": false
  },
  {
    "key": "SCARCE_SESSION_MODELS",
    "group": "quota",
    "kind": "list",
    "default": "openai/gpt-5.6-luna",
    "restart_only": false,
    "secret": false,
    "description": "1-session/day models to keep alive for their full session (never idle-evict or DELETE on shutdown while active).",
    "essential": false
  },
  {
    "key": "COST_MODE",
    "group": "upstream",
    "kind": "select",
    "default": "",
    "restart_only": false,
    "secret": false,
    "description": "",
    "enum": [
      "free"
    ],
    "essential": true
  },
  {
    "key": "HTTP2_UPSTREAM",
    "group": "upstream",
    "kind": "bool",
    "default": "true",
    "restart_only": true,
    "secret": false,
    "description": "Negotiate HTTP/2 with the upstream so the ALPN matches real browsers; false forces HTTP/1.1.",
    "essential": false
  },
  {
    "key": "MODELS_ALLOW",
    "group": "upstream",
    "kind": "list",
    "default": "",
    "restart_only": false,
    "secret": false,
    "description": "Comma-separated model allowlist: when set, only these resolved model ids are served and any other request is rejected with 404 model_not_found. Empty = all models allowed.",
    "essential": true
  },
  {
    "key": "MODELS_HIDE_UNAVAILABLE",
    "group": "upstream",
    "kind": "bool",
    "default": "false",
    "restart_only": false,
    "secret": false,
    "description": "/v1/models prunes models marked unavailable (region/tier demotion, quota exhaustion); off by default so a stale signal never hides a working model.",
    "essential": false
  },
  {
    "key": "MODEL_ALIASES",
    "group": "upstream",
    "kind": "list",
    "default": "",
    "restart_only": false,
    "secret": false,
    "description": "Map aliases to real model IDs (e.g. gpt-4o:openai/gpt-5.6-luna; comma-separated). No built-in aliases — clients must map explicitly.",
    "essential": false
  },
  {
    "key": "REGISTRY_REFRESH",
    "group": "upstream",
    "kind": "text",
    "default": "6h",
    "restart_only": false,
    "secret": false,
    "description": "Model catalog refresh interval.",
    "essential": false
  },
  {
    "key": "REQUEST_JITTER",
    "group": "upstream",
    "kind": "text",
    "default": "0s",
    "restart_only": true,
    "secret": false,
    "description": "Random delay range [0, REQUEST_JITTER) before upstream chat calls; SAFE_MODE sets 2s when unset; 0 disables.",
    "essential": false
  },
  {
    "key": "REQUEST_TIMEOUT",
    "group": "upstream",
    "kind": "text",
    "default": "15m",
    "restart_only": true,
    "secret": false,
    "description": "Upstream request timeout.",
    "essential": false
  },
  {
    "key": "SESSION_CALL_TIMEOUT",
    "group": "upstream",
    "kind": "text",
    "default": "30s",
    "restart_only": true,
    "secret": false,
    "description": "Session call timeout.",
    "essential": false
  },
  {
    "key": "TLS_FINGERPRINT",
    "group": "upstream",
    "kind": "select",
    "default": "",
    "restart_only": false,
    "secret": false,
    "description": "",
    "enum": [
      "auto",
      "chrome120",
      "chrome126",
      "safari17",
      "safari18",
      "firefox120",
      "firefox128",
      "edge126",
      "random"
    ],
    "essential": true
  },
  {
    "key": "TRANSIENT_RETRIES",
    "group": "upstream",
    "kind": "int",
    "default": "1",
    "restart_only": true,
    "secret": false,
    "description": "Max additional attempts after a transient transport failure (never retries 429/403/401; 0 disables).",
    "essential": false
  },
  {
    "key": "UPSTREAM_BASE_URL",
    "group": "upstream",
    "kind": "text",
    "default": "https://www.codebuff.com",
    "restart_only": true,
    "secret": false,
    "description": "Upstream API endpoint (codebuff.com is normalized to www.codebuff.com).",
    "essential": false
  },
  {
    "key": "ADMIN_TOKEN",
    "group": "security",
    "kind": "secret",
    "default": "123456",
    "restart_only": false,
    "secret": true,
    "description": "Login password for the admin dashboard and the bearer token POST /admin/reload requires. Defaults to the factory password 123456 — rotate it; while the factory default is active, sensitive dashboard routes additionally require a loopback client.",
    "essential": true
  },
  {
    "key": "CORS_ALLOWED_ORIGIN",
    "group": "security",
    "kind": "text",
    "default": "*",
    "restart_only": false,
    "secret": false,
    "description": "Access-Control-Allow-Origin for /v1/* responses.",
    "essential": true
  },
  {
    "key": "WEBHOOK_URL",
    "group": "security",
    "kind": "text",
    "default": "",
    "restart_only": true,
    "secret": true,
    "description": "Best-effort alert POSTs for pool_exhausted, token_banned, and agent_model_mismatch_escalation (empty = disabled; at most one POST per event type per 5m; may carry credentials in the URL userinfo).",
    "essential": true
  }
]
