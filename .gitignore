# ── third-party reference clones (CodebuffAI/freebuff, 9router, etc.) ──
reference/

# ── local secrets / credentials ──
.env
.env.local
.env.*
!.env.example
!.env.full-example
config.json
config.*.json
!config.example.json
*.pem
*.key

# ── runtime artifacts ──
dump/
*.log
dist/
!internal/dashboard/dist/
out/
bin/
tmp/
.playwright-mcp/
.ruff_cache/

# ── build outputs (Go binaries) ──
/freebuff-proxy
/freebuff-proxy.exe
/freebuff-proxy-dash.exe
*.test
*.out
coverage.out
coverage.html
*.prof

# ── editors / IDEs ──
.idea/
.vscode/
*.swp
*.swo

# ── OS cruft ──
Thumbs.db
Desktop.ini
.DS_Store

# ── git worktrees + SDD scratch ──
.worktrees/
.superpowers/

# ── misc ──
node_modules/
*.tmp
*.bak

# ── agent handoff docs (local only, not repo content) ──
HANDOFF.md
*.handoff.md

# ── dev docs (local only, not repo content); guides/ is public ──
docs/*
!docs/guides/

# ── dev study docs (local only — reverse-engineering notes, NOT public) ──
docs/guides/freebuff-cli-internals.md
docs/guides/anti-ban-gap-analysis.md

# ── proxy session persistence (SHA-256 token keys + instance state) ──
.freebuff-session-state.json
.freebuff-session-state.json.*
*.session-state.json

# local agent tool state (skills.sh installs, session artifacts)
.agents/
.claude/
skills-lock.json
